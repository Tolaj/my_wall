https://codepen.io/VoXelo/pen/XJJwjqO



            const now = new Date();
            const formatter = new Intl.DateTimeFormat('en-US', {
                timeZone: 'America/New_York', // Choose your time zone
                year: 'numeric',
                month: '2-digit',
                day: '2-digit',
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit',
            });
            const currentHour = new Date(formatter.format(now)).getHours();
